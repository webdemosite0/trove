# Copy to .env.local and fill in. .env* is gitignored — never commit real keys.

# Required. https://aistudio.google.com/apikey
# The free tier allows 20 requests per day per model; enable billing for real use.
GEMINI_API_KEY=

# Optional. Pins the first model tried; the fallback chain handles the rest.
# GEMINI_MODEL=gemini-3.5-flash

# Canonical URLs, the sitemap, the OG image, OAuth redirects and Stripe
# return URLs all derive from this.
#
# No NEXT_PUBLIC_ prefix on purpose. Nothing that reads it is a client
# component — it is metadata, robots, sitemap and server routes — so the
# prefix only inlined the value into the browser bundle, which is what Vercel
# warns about. NEXT_PUBLIC_SITE_URL is still read as a fallback so an
# existing deployment keeps working.
#
# Safe to leave blank: it falls back to Vercel's own production URL if
# present, then to http://localhost:3100. A bare domain works too —
# "troveai.site" is upgraded to "https://troveai.site".
SITE_URL=

# Required to connect integrations. Encrypts third-party credentials (GitHub
# tokens, Slack webhooks) before they are stored. Without it Trove refuses to
# save them rather than writing plaintext.
#   openssl rand -base64 32
#
# CAIRN_SECRET is still read as a fallback, so a deployment predating the
# rename keeps working until this one is set.
TROVE_SECRET=

# Database. Leave both blank for local development — it falls back to a SQLite
# file under .data/ and needs no setup.
#
# Set both to deploy anywhere with a read-only filesystem (Vercel, Netlify,
# Workers). Create a database free at https://turso.tech — no card required.
#
#   turso db create trove
#   turso db show trove --url
#   turso db tokens create trove
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Only for a self-hosted container: where the local SQLite file lives. Ignored
# when TURSO_DATABASE_URL is set. Mount a volume here or data is lost.
# TROVE_DATA_DIR=/data

# Optional. Only used by the integrations page to report live auth state.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ---------------------------------------------------------------------------
# Sign-in
# ---------------------------------------------------------------------------

# Email confirmation. Both must be set before verification is ENFORCED — with
# no mailer there would be no way to confirm an address, so new accounts are
# marked verified on creation and a warning is logged instead of bricking the
# app. Set these to turn confirmation on.
#
# https://resend.com/api-keys — the from-address must be on a domain you have
# verified with Resend. Their sandbox sender only delivers to the account
# owner, which looks exactly like "verification is broken" to everyone else.
RESEND_API_KEY=
# e.g. MAIL_FROM="Trove <official@troveai.site>" once troveai.site is verified
# with Resend.
MAIL_FROM=

# --- or send through an ordinary mailbox instead of Resend ---
#
# If the domain already has a mailbox (Spacemail, Zoho, Fastmail, Google
# Workspace), it can send the confirmation itself and Resend is unnecessary.
# Set MAIL_FROM plus these three; Resend wins if its key is also present.
#
# Spacemail, for example:
#   SMTP_HOST=mail.spacemail.com
#   SMTP_PORT=465          465 = implicit TLS, 587 = STARTTLS
#   SMTP_USER=official@troveai.site      (the full address, not a username)
#   SMTP_PASSWORD=         the mailbox password
#
# Slower than Resend, because it opens a TLS connection and holds it for the
# length of the send — fine for one confirmation email, worth revisiting if
# this ever sends in bulk.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=

# Google sign-in. The button stays disabled until both are set, because a
# sign-in option that fails on click is worse than one that is not offered.
#
# Google Cloud Console -> APIs & Services -> Credentials -> Create OAuth
# client ID -> Web application, then add this exact authorised redirect URI:
#
#   <SITE_URL>/api/auth/google/callback
#
# e.g. http://localhost:3100/api/auth/google/callback for local development.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Escape hatch. Set to 1 to run in production on storage that does not survive
# a restart — a throwaway demo or preview, where losing every account on each
# instance recycle is the intended behaviour rather than a bug. Without it the
# app refuses to serve on ephemeral storage, because that failure is silent and
# looks exactly like "refreshing logs me out and deletes my data".
TROVE_ALLOW_EPHEMERAL=

# ---------------------------------------------------------------------------
# Model fallback
# ---------------------------------------------------------------------------

# Trove tries Gemini first. When Gemini is out of quota, rate limited, or
# refusing the key, it moves down this list instead of failing — which is the
# difference between "slow today" and "the whole product stopped".
#
# All are optional and off until a key is present, so the chain collapses to
# Gemini alone by default. The switch is invisible to the user and logged for
# you, because a bill from a provider nobody knew was in use is a worse surprise
# than a slow request.
#
# One thing does not carry over: web search is Gemini's tool. Research answers
# from a fallback provider come back without sources rather than with invented
# ones.

# https://openrouter.ai/keys
OPENROUTER_API_KEY=
# Optional. Defaults to nvidia/nemotron-3.5-lightning:free, which costs
# nothing — so the fallback works on a key with no balance. Set a paid model
# here if you would rather trade money for quality.
OPENROUTER_MODEL=

# https://console.x.ai
XAI_API_KEY=
# Optional. Defaults to grok-2-latest.
XAI_MODEL=

# Puter AI — User-Pays model. Access 500+ models (GPT, Claude, Gemini, Grok,
# DeepSeek, GLM, etc.) with zero cost to the platform. Users cover their own
# usage via their Puter account.
# Create a token at https://puter.com/dashboard → Create token.
# Docs: https://docs.puter.com/ and https://developer.puter.com/ai/
PUTER_AUTH_TOKEN=
# Optional. Defaults to gpt-5.4-nano. Any model Puter exposes works.
PUTER_MODEL=

# ---------------------------------------------------------------------------
# Payments (Stripe)
# ---------------------------------------------------------------------------

# All optional. With STRIPE_SECRET_KEY unset, paid plans render as "Not
# available yet" instead of offering a Subscribe button that would fail on
# click, and the free plan keeps working as before.
#
# https://dashboard.stripe.com/apikeys — use a test key (sk_test_...) until
# you are ready to take real money.
STRIPE_SECRET_KEY=

# Signing secret for the webhook. This is what proves a request really came
# from Stripe, and it is the ONLY thing that grants a paid plan — without it
# the webhook returns 503 and subscriptions never activate.
#
# Add an endpoint at <SITE_URL>/api/billing/webhook subscribed to:
#   checkout.session.completed
#   customer.subscription.created
#   customer.subscription.updated
#   customer.subscription.deleted
#
# Locally, use the CLI instead — it prints a whsec_ for the forwarded session:
#   stripe listen --forward-to localhost:3100/api/billing/webhook
STRIPE_WEBHOOK_SECRET=

# One recurring price id per paid plan, from Stripe -> Product catalogue.
# These are account-specific and differ between test and live mode, which is
# why they are configuration rather than code. A plan whose price is missing
# stays unbuyable instead of charging the wrong amount.
#
# The name is STRIPE_PRICE_<PLAN ID UPPERCASED>, matching the plans in
# src/lib/credits.ts.
STRIPE_PRICE_PRO=
STRIPE_PRICE_TEAM=

# ---------------------------------------------------------------------------
# Web search
# ---------------------------------------------------------------------------

# A model's knowledge is frozen at its training date. Telling it today's date
# only lets it say "my information may be out of date" — the only way it can
# actually answer a question about now is to look something up.
#
# Gemini has grounding built in and Trove prefers it, but Google meters it
# separately from the model and refuses it on a free key. So Trove can also run
# the search itself: the model asks via a function call and Trove queries the
# provider configured below.
#
# All of these are optional and only the first one configured is used. With
# none set it falls back to Wikipedia, which needs no key and is edited within
# hours — good enough for "who runs X now" or "what is X's net worth", useless
# for a share price or this morning's news. Set one of the others for real web
# search.
#
# The model only ever supplies a search *query*. It cannot ask Trove to fetch
# a URL, so there is no address it can point this server at.

# https://brave.com/search/api — 2,000 queries/month free
BRAVE_API_KEY=

# https://tavily.com — built for this, 1,000 credits/month free
TAVILY_API_KEY=

# https://serper.dev — Google results, 2,500 free credits
SERPER_API_KEY=

# https://developers.google.com/custom-search — 100 queries/day free.
# Needs both: the API key and the search-engine id.
GOOGLE_CSE_KEY=
GOOGLE_CSE_ID=
