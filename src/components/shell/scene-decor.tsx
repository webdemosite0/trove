/**
 * Scene decorations for the home / chat empty state.
 * Glass cube, abstract orb, and handwritten notes removed for a cleaner hero.
 * Kept as a no-op so existing imports continue to resolve.
 */
export function SceneDecor() {
  return null;
}
