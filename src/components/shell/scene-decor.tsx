/**
 * Scene decorations for the home / chat empty state.
 * Glass cube, abstract orb, and handwritten notes were removed
 * for a cleaner hero — component kept as a no-op for any remaining imports.
 */
export function SceneDecor() {
  return null;
}
